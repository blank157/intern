/** STYLE: EvalAI subject entry — a transparent teacher-authored field makes the comma-separated workflow explicit and calm. */
import { BookOpenText } from "lucide-react";
import { parseSubjectNames } from "./subject-utils";

export function CommaSeparatedSubjectsField({ value, onChange, error, description = "Type each subject you teach. If you teach multiple subjects, separate them with commas." }: { value: string; onChange: (value: string) => void; error?: string; description?: string }) {
  const subjects = parseSubjectNames(value);
  return <div><div className="flex items-baseline justify-between gap-3"><p className="text-[12px] font-bold text-[#171827]">Subjects you teach <span className="text-[#7788ff]">*</span></p>{subjects.length > 0 && <span className="text-[11px] font-semibold text-[#6679e8]">{subjects.length} subject{subjects.length === 1 ? "" : "s"}</span>}</div><p className="mt-1 text-[11px] font-medium leading-4 text-slate-500">{description}</p><div className={`relative mt-2 rounded-2xl border bg-white/78 transition-colors ${error ? "border-[#d97b77]" : "border-[#dfe4ff] focus-within:border-[#7788ff] focus-within:ring-2 focus-within:ring-[#b6c0ff]/35"}`}><BookOpenText className="pointer-events-none absolute left-3.5 top-3.5 h-4 w-4 text-[#7788ff]" /><textarea required value={value} onChange={(event) => onChange(event.target.value)} placeholder="Machine Learning, Artificial Intelligence, Deep Learning" rows={3} className="w-full resize-none rounded-2xl bg-transparent py-3 pl-10 pr-3 text-[13px] font-semibold leading-5 text-[#171827] outline-none placeholder:font-medium placeholder:text-slate-400" /></div>{subjects.length > 0 && <div className="mt-2 flex flex-wrap gap-1.5">{subjects.map((subject) => <span key={subject} className="rounded-xl border border-[#d9e0ff] bg-[#f4f6ff] px-2.5 py-1 text-[11px] font-bold text-[#586bda]">{subject}</span>)}</div>}{error && <p className="mt-1.5 text-[11px] font-semibold text-[#b85d59]">{error}</p>}</div>;
}
