/** STYLE: Measured Softness — advanced settings are tucked into a clear, optional evaluation control plane. */
import { ChevronDown, SlidersHorizontal } from "lucide-react";
import { useState } from "react";
import { ComparisonSettings as ComparisonSettingsType } from "@/lib/compare";
interface ComparisonSettingsProps { settings: ComparisonSettingsType; onChange: (settings: ComparisonSettingsType) => void; }
export function ComparisonSettings({ settings, onChange }: ComparisonSettingsProps) { const [open, setOpen] = useState(false); const normalized = settings.mode === "normalized"; const options: Array<{ key: keyof Pick<ComparisonSettingsType, "ignoreCase" | "ignoreWhitespace" | "ignoreLineBreaks" | "ignorePunctuation">; label: string; detail: string }> = [{ key: "ignoreCase", label: "Ignore capitalization", detail: "Treat Hello and hello as aligned." }, { key: "ignoreWhitespace", label: "Ignore extra whitespace", detail: "Collapse repeated spacing before review." }, { key: "ignoreLineBreaks", label: "Ignore line breaks", detail: "Treat line breaks as spaces during review." }, { key: "ignorePunctuation", label: "Ignore punctuation", detail: "Remove punctuation and symbols during alignment." }]; return <section className="rounded-2xl border border-slate-200/80 bg-white dark:border-white/10 dark:bg-[#1e2131]"><button type="button" onClick={() => setOpen((value) => !value)} className="flex w-full items-center justify-between gap-3 px-4 py-3.5 text-left focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#7788ff]" aria-expanded={open}><span className="flex items-center gap-2"><span className="flex h-7 w-7 items-center justify-center rounded-lg bg-[#eef0ff] text-[#6476e8] dark:bg-[#7587f2]/15 dark:text-[#aeb8ff]"><SlidersHorizontal className="h-3.5 w-3.5" /></span><span><span className="block text-sm font-extrabold text-[#171827] dark:text-white">Answer alignment settings</span><span className="block text-xs font-medium text-slate-500 dark:text-slate-400">Optional review rules for expected and student answers.</span></span></span><ChevronDown className={`h-4 w-4 text-slate-500 transition-transform duration-150 ${open ? "rotate-180" : ""}`} /></button>{open && <div className="border-t border-slate-100 px-4 py-4 dark:border-white/10"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-extrabold uppercase tracking-[0.13em] text-slate-500 dark:text-slate-400">Alignment mode</p><p className="mt-1 text-xs font-medium text-slate-500 dark:text-slate-400">Exact review retains every character. Flexible review applies selected rules.</p></div><div className="flex rounded-xl bg-slate-100 p-1 dark:bg-white/5" role="group" aria-label="Answer alignment mode">{(["strict", "normalized"] as const).map((mode) => <button key={mode} type="button" onClick={() => onChange({ ...settings, mode })} className={`rounded-lg px-3 py-1.5 text-xs font-extrabold transition-all ${settings.mode === mode ? "bg-white text-[#171827] shadow-sm dark:bg-[#7688f4] dark:text-[#151827]" : "text-slate-500 dark:text-slate-400"}`}>{mode === "strict" ? "Exact" : "Flexible"}</button>)}</div></div><div className={`mt-4 grid gap-2 sm:grid-cols-2 ${normalized ? "" : "opacity-55"}`}>{options.map((option) => <label key={option.key} className="flex cursor-pointer items-start gap-3 rounded-xl border border-slate-100 p-3 transition-colors hover:border-[#d9ddff] dark:border-white/10 dark:hover:border-[#8b9aff]/25"><input type="checkbox" checked={settings[option.key]} disabled={!normalized} onChange={(event) => onChange({ ...settings, [option.key]: event.target.checked })} className="mt-0.5 h-4 w-4 rounded border-slate-300 text-[#6c7ff0] focus:ring-[#7788ff] disabled:cursor-not-allowed" /><span><span className="block text-xs font-extrabold text-slate-700 dark:text-slate-200">{option.label}</span><span className="mt-0.5 block text-[11px] font-medium leading-4 text-slate-500 dark:text-slate-400">{option.detail}</span></span></label>)}</div></div>}</section>; }
